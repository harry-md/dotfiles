return {
  "nvim-treesitter/nvim-treesitter-context",
  opts = {
    enable = true,
    multiwindow = true,
    max_lines = 6,
    min_window_height = 0,
    line_numbers = false,
    multiline_threshold = 2,
    trim_scope = "outer",
    mode = "cursor",
    separator = nil,
    zindex = 20,
    on_attach = nil,
  },
}
